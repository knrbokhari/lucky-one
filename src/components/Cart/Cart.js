import React from "react";
import "./Cart.css";

const Cart = (props) => {
  return (
    <div>
      <p>{props.cartItem.name}</p>
      {/* <a href="" onClick={kjkl}>abc</a> */}
    </div>
  );
};

export default Cart;
